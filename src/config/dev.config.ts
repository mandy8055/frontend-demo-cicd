/* Config file for local development. This is WIP.
 * Need to figure out better ways to manage this
 */

const devConfig = {
  authServiceBaseURL: 'http://localhost:8081',
  holdingsServiceBaseURL: 'http://localhost:8080',
  // ...so on
};

export default Object.freeze(devConfig);
